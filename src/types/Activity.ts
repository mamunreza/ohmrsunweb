export interface Activity {
    id?: string;
    title: string;
    description?: string;
    activityDate: string; // combined date and time
    // cost?: number;
    // createDate?: string;
    // createdBy?: string;
    // modifiedDate?: string;
    // modifiedBy?: string;
  }